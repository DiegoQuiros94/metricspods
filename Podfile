# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Metrics-with-pods' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Metrics-with-pods

  pod 'Alamofire', '~> 5.2'

  target 'Metrics-with-podsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Metrics-with-podsUITests' do
    # Pods for testing
  end

end
